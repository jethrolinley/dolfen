=============
Bilby example
=============

``dolfen`` can be subclassed with the standard bilby Likelihood class and simply passed to ``bilby`` as a likelihood function. The following example shows how one can do so.

Code
====

.. literalinclude:: ../examples/bilby_PE.py
    :language: python
